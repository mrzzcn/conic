class Point {
  x: number;
  y: number;

  /**
   * Creates an instance of Point.
   * @param {number} x
   * @param {number} y
   * @memberof Point
   */
  constructor(x: number, y: number) {
    this.x = x;
    this.y = y;
  }
}

export default Point;